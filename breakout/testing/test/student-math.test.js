const assert = require('chai').assert;
const math = require('../student-math');

describe('User', function () {
  describe('firstName', function () {
    it('is not a blank String', function () {
      // test code here
    });
    // additional tests for firstName
  });
});
